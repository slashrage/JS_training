class Chair {
  constructor(height, width, armrests, material, color) {
    (this.height = height),
      (this.width = width),
      (this.armrests = armrests),
      (this.material = material),
      (this.color = color);
  }
}

export default Chair;
